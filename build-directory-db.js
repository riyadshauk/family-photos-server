/**
 * @todo recursively traverse sample-photos dir, replicating the hierarchy and creating a db (JSON) with an entry
 *  for each non-folder file in each sub-folder. 
 *  Each entry may contain data about the photo (eg: comments, likes, exif, owner, etc).
 * 
 * @todo alternatively, keep everything in a centralized SQL db by following 
 * this advice: https://dba.stackexchange.com/questions/226471/hierarchical-tree-data-which-type-of-database-to-use-design
 */

